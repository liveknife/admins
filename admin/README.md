# Admins 前端

基于 Vue 3、Vite、TypeScript、Element Plus 构建的后台管理系统前端。

## 仓库

https://github.com/liveknife/admins

## 启动

```bash
pnpm install
pnpm dev
```

## 说明

本目录是 `admins` 项目的前端代码，后端代码位于根目录下的 `go_demo`。
